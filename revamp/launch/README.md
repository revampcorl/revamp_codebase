# Utility Layout

- `config.py`: load JSON configs, expand environment variables, and resolve release-local paths.
- `files.py`: small file-system helpers used by launch pipelines.
- `console.py`: command-line display helpers.
- `pipeline.py`: materialize configs and build or execute the release launch graphs for imagination policy and online world-model retraining.
